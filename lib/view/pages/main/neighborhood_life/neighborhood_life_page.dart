import 'package:flutter/material.dart';

class NeighborhoodPage extends StatelessWidget {
  const NeighborhoodPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("NeighborhoodPage"),
    );
  }
}
